/:
  controller: general
  action: home

/login:
  controller: user
  action: login

/logout:
  controller: user
  action: logout

/register:
  controller: user
  action: register

/activate/:token:
  controller: user
  action: activate

/reset/:token:
  controller: user
  action: reset

/reset:
  controller: user
  action: reset

# Admin routes

/dashboard:
  controller: admin
  action: dashboard
  protected: true

/orders:
  controller: admin
  action: orders
  protected: true

/admin/products:
  controller: admin
  action: products
  protected: true

/admin/users:
  controller: admin
  action: users
  protected: true

/admin/shop:
  controller: admin
  action: shop
  protected: true

/admin/pages:
  controller: admin
  action: pages
  protected: true

/admin/menus:
  controller: admin
  action: menus
  protected: true