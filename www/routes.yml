/:
  controller: general
  action: home

/login:
  controller: user
  action: login

/logout:
  controller: user
  action: logout

/register:
  controller: user
  action: register

# Admin routes

/dashboard:
  controller: admin
  action: dashboard
  protected: true
